// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int pom(int arg){
        int n2=arg;
    int brcif=0;
    int res;
    while(n2!=0){
        brcif++;
        n2/=10;
    }
    
    if (brcif%2==0){
        int sredina1 = brcif/2+1;
        int sredina2 = brcif/2;
        int br1 = arg/pow(10,sredina1-1);
        br1%=10;
        int br2 = arg/pow(10,sredina2-1);
        br2%=10;
        if (br1>br2) res = br1;
        else res = br2;
    }
    else {
        int sredina = brcif/2+1;
        res = arg/pow(10,sredina-1) ;
        res = res%10;
    }
    return res;
}
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    for (int i=a;i<=b;i++){
        int sred = pom(i);
        if (sred!=0)
        if (i%(sred*sred*sred)==0){
            printf("%d ",i);
        }
    }
}
